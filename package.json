{
  "name": "llmsdottxt",
  "version": "1.0.0",
  "description": "Chrome extension to detect and access llms.txt files",
  "scripts": {
    "build:icons": "node scripts/build-icons.js",
    "build": "npm run build:icons",
    "test": "npx playwright test",
    "test:coverage": "npx c8 npx playwright test",
    "test:e2e": "npx playwright test tests/e2e/"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "c8": "^8.0.0",
    "sharp": "^0.33.0"
  },
  "type": "module"
}
